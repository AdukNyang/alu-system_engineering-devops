# Web Stack Debugging #0

This project involves debugging a Docker container running Apache2.
The goal is to get Apache to return a page containing "Hello Holberton" when querying the root.

## Fix
Apache2 was not running inside the container. Starting the service fixed the issue.
